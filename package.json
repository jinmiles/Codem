{
  "name": "codem",
  "version": "0.1.0",
  "description": "Cross-platform Codex usage meter",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "check": "tsc --noEmit",
    "test": "npm run check && cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
